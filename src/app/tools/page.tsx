import type { Metadata } from "next";
import Link from "next/link";
import { toolDefinitions } from "./data";

export const metadata: Metadata = { title:"Free AI Tools Directory", description:"Explore practical AI tools for marketing, SEO, business, content creation, productivity and growth.", alternates:{canonical:"/tools"} };

export default function ToolsPage(){return <main className="page-shell"><section className="directory-hero"><span className="eyebrow">AI Tools Directory</span><h1>Practical AI tools for the work that drives growth.</h1><p>Explore focused generators and workflows for marketing, SEO, content, business and productivity. Start free and move from input to usable output in seconds.</p><div className="hero-actions"><Link className="button primary" href={`/tools/${toolDefinitions[0].slug}`}>Try a tool</Link><Link className="button secondary" href="/use-cases">Explore use cases</Link></div></section><section className="section-block"><div className="section-heading row"><div><span className="eyebrow">Featured workflows</span><h2>Start with a real task.</h2><p>Every listed tool opens a working AI workspace rather than looping back to the directory.</p></div><Link className="text-link" href="/pricing">Compare plans →</Link></div><div className="tool-grid">{toolDefinitions.map((tool,i)=><article className="tool-card" key={tool.slug}><div className="tool-card-top"><span className="tool-icon">{i+1}</span><span className="badge subtle">{tool.category}</span></div><h3>{tool.name}</h3><p>{tool.description}</p><Link className="button secondary full" href={`/tools/${tool.slug}`}>Open tool</Link></article>)}</div></section></main>}
