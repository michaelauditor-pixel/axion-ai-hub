const SITE_URL="https://axionaihub.com";const HOST="axionaihub.com";const KEY="c975cd84baa4601593072499fc5c2f39";const KEY_LOCATION=`${SITE_URL}/${KEY}.txt`;export const runtime="nodejs";export const dynamic="force-dynamic";
function normalizeUrl(v:string){try{const u=new URL(v,SITE_URL);if(u.hostname!==HOST)return null;u.hash="";return u.toString()}catch{return null}}
export async function GET(){return Response.json({ok:true,service:"IndexNow",keyLocation:KEY_LOCATION,maxBatch:10000})}
export async function POST(req:Request){try{const body=await req.json().catch(()=>({}));const raw=Array.isArray(body.urls)?body.urls:body.url?[body.url]:[];const urlList=raw.map((u:unknown)=>normalizeUrl(String(u))).filter(Boolean).slice(0,10000) as string[];if(!urlList.length)return Response.json({ok:false,error:"Provide at least one axionaihub.com URL."},{status:400});const r=await fetch("https://api.indexnow.org/indexnow",{method:"POST",headers:{"Content-Type":"application/json; charset=utf-8"},body:JSON.stringify({host:HOST,key:KEY,keyLocation:KEY_LOCATION,urlList})});return Response.json({ok:r.ok||r.status===202,status:r.status,submitted:urlList.length},{status:r.ok||r.status===202?200:502})}catch(e){return Response.json({ok:false,error:e instanceof Error?e.message:"IndexNow request failed"},{status:500})}}
